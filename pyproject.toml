[project]
name = "training-set-classifier-georeset"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "trafilatura>=2.1.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.0",
]
